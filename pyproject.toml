[project]
name = "modaic-examples"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "modaic-client>=0.40.0",
    "datasets>=4.8.5",
    "modaic>=0.40.0",
]
